const double h(0.1); //pas de temps h = 0.1 ms
const double tau(10); //tau = R*C ms
const double tauRef(2); //tau refractory 2 ms
const double energieRepos(-70); //-70mV
const double potentialReset(-70); //-70 mV Potntiel de membrane pendant le refractory time
const double capacite(250); // 250 pF=10⁻12 Farad

